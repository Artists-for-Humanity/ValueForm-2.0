{
  "name": "ValueForm-Chris_Pross",
  "version": "2.0.0",
  "description": "Artist For Humanity Client Work- ValueForm Christopher Pross",
  "main": "script.js",
  "scripts": {
    "start": "node server.js",
    "test": "percy exec -- cypress run",
    "build": "rm -rf dist && mkdir -p dist && cp -r pages dist/pages && cp -r index.html dist && cp -r css dist/css && cp -r images dist/images && cp -r scripts dist/scripts",
    "vercel-build": "npm run build",
    "check-node": "node -v"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.3",
    "express": "^4.20.0",
    "path-to-regexp": "^8.1.0",
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "send": "^0.19.0",
    "serve-static": "^1.16.2",
    "styled-components": "^6.1"
  },
  "devDependencies": {
    "@babel/core": "^7.24.7",
    "@babel/preset-env": "^7.24.7",
    "@percy/cli": "^1.30.7",
    "@percy/cypress": "^3.1.4",
    "babel-loader": "^9.1.3",
    "cypress": "^14.1.0",
    "webpack": "^5.92.0",
    "webpack-cli": "^5.1.4"
  }
}