{
  "name": "ValueForm-Chris_Pross",
  "version": "2.0.0",
  "description": "Artist For Humanity Client Work- ValueForm Christopher Pross",
  "main": "script.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd sanity-studio && npx sanity build --output-path=dist && cd .. && mkdir -p dist && cp index.html dist && echo 'Copied main site index.html' && cp -r pages dist/pages && cp -r css dist/css && cp -r images dist/images && cp -r scripts dist/scripts && mkdir -p dist/sanity-studio && cp -r sanity-studio/dist/* dist/sanity-studio/ && cp sanity-studio/dist/index.html dist && cp sanity-studio/fetchTagline.js dist/sanity-studio/ && cp sanity-studio/sanity-utils.js dist/sanity-studio/ && echo 'Build complete'",
    "vercel-build": "npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@sanity/client": "^6.20.0",
    "@sanity/image-url": "^1.0.2",
    "express": "^4.18.2",
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "sanity-plugin-vercel-deploy": "^3.3.4",
    "styled-components": "^6.1"
  },
  "devDependencies": {
    "@babel/core": "^7.24.7",
    "@babel/preset-env": "^7.24.7",
    "babel-loader": "^9.1.3",
    "sanity": "^3.0.0",
    "webpack": "^5.92.0",
    "webpack-cli": "^5.1.4"
  }
}
